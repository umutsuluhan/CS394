<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectt">


    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/elma"
        android:logo="@drawable/elma"
        android:label="@string/app_name"
        android:roundIcon="@drawable/elma"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projectt">
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MainPageActivity" />
        <activity android:name=".activities.CateringActivity" />
        <activity android:name=".activities.PrivateLessonActivity" />
        <activity android:name=".activities.RenovationActivity" />


        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>


</manifest>